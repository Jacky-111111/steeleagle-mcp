[project]
name = "steeleagle-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "grpcio>=1.80.0",
    "mcp>=1.27.1",
    "steeleagle-sdk",
]

[tool.uv.sources]
steeleagle-sdk = { path = "../steeleagle/sdk" }
